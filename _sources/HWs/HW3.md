# Homework 3 
* *Do your cognitively demanding attempt and write it down **before** you ask an AI Chatbot*. 
* *Homeworks are long, start early!*
* *Check Due Date, Office Hours, and Textbook on Canvas*  



1. Justify the answer (Hint: use $P(\Omega) = 1$) 
    *  $g(x) = 1 \,\, \forall \,\, a \in X; X  = [0,1]$. Is the function $g$ a pdf, a pmf, or neither? 
    *  $g(x) = \frac{1}{2} \,\, \forall \,\, a \in X$; $X = \{1,2,3\}$ . Is the function $g$ a pdf, a pmf, or neither?
    *  $g(x) = |x| \,\, \forall \,\, a \in X; X = [-1,1] $. Is the function $g$ a pdf, a pmf, or neither? 
        *Note: $|x| = \begin{cases} x, \,\, if \,\, x \geq 0 \\ -x, \,\, if \,\, a < 0 \\ \end{cases} $ (known as absolute value function)*   
    *   $g(x) = \frac{1}{\sqrt{2\pi}}\exp(-\frac{a^2}{2}) \forall \,\, x \in X; X = (-\infty,\infty)$. Is the function $g$ a pdf, a pmf, or neither?
    *  $g(x) =$ $n \choose x$ $p^x(1-p)^{n-x} \,\, \forall \,\, x \in X; X = \{0,1,2,...,n\}$. Is the function $g$ a pdf, a pmf, or neither?


2. *(Long problem but great experience!)* Derive mathematically, 
    * Part 1: $\mathbb{E}[X]$ (the expected value of $X$) if 
        * $X \sim Bernoulli(p)$
        * $X \sim Poisson(\lambda)$
        * $X \sim Uniform(a,b)$
        * $X \sim Exponential(\lambda)$
    * Part 2: $\big(\mathbb{E}[X^2] - (\mathbb{E}[X])^2\big)$, if *(hint: use $\mathbb{E}[X]$ from previous part)*
        * $X \sim Bernoulli(p)$
        * $X \sim Poisson(\lambda)$
        * $X \sim Uniform(a,b)$
        * $X \sim Exponential(\lambda)$

3. For a uniform distribution $U(5,8)$ 
    * What is the probability $p(5.5)$?
    * What is $f(5.5)$?
    * What are $F(0.2)$, $F(5.5)$, and $F(8.5)$? 


4. Sketch the pdfs and cdfs of $Bernoulli(0.3)$, $Bernoulli(0.5)$, and $Bernoulli(0.75)$.

5. Let there are $n$ random variables, such that $X_1 \sim Bernoulli(0.6)$, $X_2 \sim Bernoulli(0.6)$,$\cdots$, $X_n \sim Bernoulli(0.6)$. What is the $\mathbb{E}[X]$ where $X = X_1 + X_2 + X_3+ \cdots + X_n$?  

6. Textbook Section 3.2 \#11 (No need to draw 'probability histogram' in (b))

7. Textbook Section 3.2 \#24 

8. Textbook Section 3.2 \#26 
    
9. Textbook Section 3.3 \#30 

10. Textbook Section 3.3 \#38


## Additional Questions (Optional)
1. Prove that the 
    * $Poisson(\lambda)$ is a probability mass function. 
    * $Exponential(\lambda)$ is a probability distribution function.


2. $P(x) =$ $4 \choose x$ $0.3^x(0.7)^{4-x} \,\, x \in X, \,\, X = \{0,1,2,3,4\}$ , find *(note the similarity of equation to last part of Problem 1)*
    * $\mathbb{E}[X]$
    * $\mathbb{E}[X^2] - (\mathbb{E}[X])^2$
